/**
 * Shared layout timing constants for the Bible reader.
 */

/** Delay before re-keying verse content after a panel opens/closes/collapses, to wait out the CSS transition. */
export const LAYOUT_REKEY_MS = 350;
