{
  "name": "biblemarker-modules-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker that authenticates Bible module downloads for the official BibleMarker app.",
  "scripts": {
    "dev": "wrangler dev",
    "publish": "wrangler deploy --env production",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "~6.0.2",
    "vitest": "^4.0.18",
    "wrangler": "^4.0.0"
  }
}
